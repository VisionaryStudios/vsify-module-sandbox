<!-- Link the issue HERE, never in a commit message (see CONTRIBUTING.md). Cross-repo is fine:
     `Closes VisionaryStudios/vsify-enterprise-mcp#NNN` -->
Closes #

## What changed

## Cross-repo checklists (CONTRIBUTING.md)

- [ ] This PR does **not** touch `schemas/SANDBOX_WIRE.json`, `vsify_sandbox/framing.py`, or the
      frame layout documented in `README.md`.
- [ ] …or it does, and steps `W1`–`W9` are complete, with the companion vsify-enterprise-mcp PR
      linked below and both `shasum -a 256` values pasted in.
- [ ] This PR does **not** change the `FROM` line in `Dockerfile`.
- [ ] …or it does, and steps `B1`–`B3` are complete (multi-arch index digest confirmed, `verify`
      green).

Companion PR:
